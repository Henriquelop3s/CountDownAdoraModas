import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  reactStrictMode: true,
  experimental: {
    // analyticsId: 'XXXXXXXXXXXX', // Remova esta linha, se existir
  },
};

export default nextConfig;
